/** Public bucket for `customer_profiles.master_logo_path` (see migration 20260503). */
export const CUSTOMER_MASTER_LOGO_BUCKET = "customer-master-logos";

/** Max upload size for master logo (images + PDF). */
export const MAX_CUSTOMER_MASTER_LOGO_BYTES = 15 * 1024 * 1024;
